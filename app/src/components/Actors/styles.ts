import styled from 'styled-components';

export const Heading = styled.h1`
  text-decoration: underline;
`;

export const ActorContainer = styled.div`
  border-bottom: 1px solid #0d050e;
  padding: 2rem 0rem;

  &:last-child {
    border-bottom: none;
  }
`;
